apiVersion: 1
contactPoints:
  - orgId: 1
    name: grafana-default-email
    receivers:
      - uid: email-alerts
        type: email
        settings:
          addresses: "{{ALERT_EMAIL_RECIPIENT}}"
          from_name: "Grafana {{ALERT_EMAIL_SITE_ID}}"
          subject: "[{{ALERT_EMAIL_SITE_ID}}] {{ .Title }}"