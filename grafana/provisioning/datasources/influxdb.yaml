apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    isDefault: true
    url: 
    jsonData:
      version: Flux
      organization: my-org
      defaultBucket: pac2200
      tlsSkipVerify: true
    secureJsonData:
      token: my-token
