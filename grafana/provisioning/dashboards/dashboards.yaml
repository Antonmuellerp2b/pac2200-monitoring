# dashboards.yaml - Grafana dashboard provisioning
# This file configures the default dashboard provider for file-based dashboards

apiVersion: 1

providers:
  - name: "default"
    folder: ""
    type: file
    disableDeletion: false
    editable: true
    options:
      path: /etc/grafana/provisioning/dashboards
